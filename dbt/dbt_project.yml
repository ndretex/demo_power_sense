name: demo_power_sense
version: 1.0.0
config-version: 2

profile: demo_power_sense

model-paths: [models]
analysis-paths: [analyses]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

clean-targets:
  - target
  - dbt_packages

models:
  demo_power_sense:
    electricity:
      staging:
        +materialized: view
        +tags: [staging]
      intermediate:
        +materialized: view
      marts:
        +materialized: table
        +tags: [mart]
